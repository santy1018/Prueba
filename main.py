from controlador import Controlador

if __name__ == "__main__":
    controlador = Controlador() #se instancia la clase Controlador
    controlador.run() #invocacion del metodo "run" que contiene las funciones de vista y modelo

#Nos permite que nuestro programa se ejecute
